{
  "name": "aspect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "try": "npm run tsc:lib && node --inspect lib/index",
    "tsc:lib": "tsc --project ./lib/tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "jison": "(cd compiler ; jison grammar2.json && node index.js)"
  },
  "author": "Brandon Smith",
  "license": "ISC",
  "dependencies": {
    "jison": "^0.4.18",
    "mobx": "^5.1.2"
  },
  "devDependencies": {
    "typescript": "^3.1.3"
  }
}
