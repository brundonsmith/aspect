{
  "name": "aspect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "try": "npm run tsc:lib && node --inspect library2/index",
    "tsc:lib": "tsc --project ./library2/tsconfig.json",
    "tsc:tok": "tsc ./tokenizer/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brandon Smith",
  "license": "ISC",
  "dependencies": {
    "mobx": "^5.1.2"
  },
  "devDependencies": {
    "typescript": "^3.1.3"
  }
}
